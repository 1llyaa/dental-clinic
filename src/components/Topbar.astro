---
import { getTranslations, type Locale } from '../i18n/utils';

interface Props { locale?: Locale; }
const { locale = 'cs' } = Astro.props;
const t = getTranslations(locale);
---
<div class="topbar">
  {t.topbar.text} <a href={t.site.phoneLink}>{t.topbar.cta}</a>
</div>
